name: event_repository
description: Dart package which manages the events domain.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.0
  cloud_firestore: ^2.3.0
  firebase_core: ^1.0.0
  meta: ^1.3.0
  very_good_analysis: ^2.1.0
  rxdart: ^0.27.1
  flutter_local_notifications: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.1
